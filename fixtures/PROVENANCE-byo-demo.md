# PROVENANCE — `byo-demo-trace.json` (ADR-M006 D10, recorded bring-your-own demonstration)

Traceable origin for `fixtures/byo-demo-trace.json`, the committed recording of the MONARK harness
driven END-TO-END over its real MCP surface through the **bring-your-own (BYO)** loop (ADR-M006 D10;
the BYO loop of `skills/monark/SKILL.md`). This file is English by ADR-M003 D0.5. The trace is a NEW
English artifact (not a Shōgen verbatim), so — unlike the `s3-binance.*` fixtures — it is NOT
path-exempt: it passes `lang-gate --scope root` and `grep-forbidden.mjs`. The pinned digest below is
what `fixtures/manifest.json` does for the nine `*.gate-decision.json` states; that manifest is a
closed set (enforced by `test/fixtures_root_valid`), so this trace is pinned HERE instead, the same way
`h5-e2e-trace.json` is pinned in `PROVENANCE-h5-e2e-trace.md`.

- **Generated by**: the MONARK recorder on 2026-09-11 (grounding: ADR-M006 D10). Recorder:
  `scripts/record-byo-demo.mjs` (which drives the shared `test/byo-demo-builder.ts`).
- **Reviewer**: independently reviewed and recorded before commit.

## What it records

The recorder stands the harness up IN-PROCESS on a real `127.0.0.1` ephemeral listener (`startServer`)
and drives a real `initialize`, then two `tools/call` JSON-RPC requests over the streamable-HTTP
transport (the response is framed as `text/event-stream`), NOT `tool.run()` calls:

1. **`calibrate`** — a fictitious third-party caller's OWN nonconformity scores `[0.1..1.0]` (n=10) at
   miscoverage α=0.1, nMin=5. MONARK returns the split-conformal quantile `qhat` and a `set_digest`
   over exactly those scores. The scores are ILLUSTRATIVE (not measured) and name no asset.
2. **`gate`** — the caller's OWN prediction (ŷ=0, a caller-owned `task_class` `caller-demo-reg`, never
   a committed class) with `params.calibration` = the SAME scores in `interval` mode. The gate
   conformalizes against THOSE caller-supplied scores (BYO) and returns a covered `commit`.

## The honest decision the demo produces

- **calibrate: q̂ = 1.0.** `p = ⌈(n+1)(1−α)⌉ = ⌈11·0.9⌉ = 10`; q̂ = the 10th smallest score = 1.0.
- **gate: `commit` / `covered`.** Region `[ŷ−q̂, ŷ+q̂] = [−1, 1]`; width 2 < `tauInterval` 3 and the
  intent 0 lies in `[−1, 1]` ⇒ a clear commit (visible margin). `allow` is a coverage verdict on the
  caller's prediction, NOT permission to execute the named tool; the gate never calls the named tool.
- **the audit closes.** The gate verdict's `calib_digest` equals the calibrate `set_digest`
  (`4081f718…`) over the same scores.
- **B_t is caller-carried.** `remainingBudget` (0.1) enters as a gate parameter and the SAME value
  leaves as `remaining_budget`. The stateless server does NOT deplete it, sees no asset, and stores
  nothing between the two calls.

Marginal 1−α coverage holds ONLY under exchangeability of the caller's future points with these
scores; it is never a probability of being right. These statements reside in the trace's `honesty` block
and are re-asserted by `test/byo-demo-probe.test.ts`.

## Determinism and integrity

- The tools read no clock (`produced_at` is caller-carried); the ephemeral port is intentionally NOT
  recorded. Re-running the recorder reproduces the file byte-for-byte.
- **sha256 (LF-normalized)**: `79b54471bb2e4d92f574e6456f3f4d548f7d73f187ea2528cbc54ca6f49286d4`
  (9575 bytes). `.gitattributes` normalizes to `eol=lf`, so this digest survives commit; the probe
  LF-normalizes before hashing, so it also survives a CRLF checkout. This value is pinned as
  `TRACE_SHA256_PINNED` in `test/byo-demo-probe.test.ts`.
- Byte-reproducible: two consecutive recorder runs printed the identical LF sha256
  (`79b54471…`), and `probe_byo_demo_loop_closes` re-derives it in-test. Regenerate, never hand-edit:
  `node scripts/record-byo-demo.mjs`.
- **No `tools/list` step** is recorded on purpose: unlike `h5-e2e-trace.json` (which re-pinned three
  times as the tool schemas evolved), this trace records only `initialize` + the two `tools/call`
  results, so it does not move when an unrelated tool's input schema changes. The two `tools/call`
  results and every digest are the demo's whole content.

## How the probe is mock-discriminating

`probe_byo_demo_loop_closes` re-drives the SAME chain in-process and `deepEqual`s it to this committed file
(faithfulness), pins the LF sha256 (tamper-evidence), then — because a replayed trace could copy the
demo values — proves the audit tie THREE independent ways and drives TEST-CHOSEN PERTURBED inputs that
are NOT in this trace over the same wire seam (`byoToolsCall`):

- the audit tie `gate.verdict.calib_digest == calibrate.set_digest` is also checked against
  `runCalibrate(scores).set_digest` recomputed from the raw scores;
- q̂ and the region `[−1, 1]` are checked against a HAND-ROLLED conformal quantile (p-th smallest by
  repeated min-extraction, ≠ production `splitQuantile`);
- `calibrate` at a perturbed α=0.5 ⇒ q̂=0.6 (the demo holds only q̂=1.0), so a frozen/mock trace reds;
- `gate` at a perturbed ŷ=5 ⇒ region `[4, 6]` (the demo holds only `[−1, 1]`), so a frozen trace reds;
- `calibrate`+`gate` on a DIFFERENT score set ⇒ a DIFFERENT digest that STILL ties, so the tie is not
  a constant.

## Reproduce

```
node scripts/record-byo-demo.mjs      # rewrites fixtures/byo-demo-trace.json, prints the LF sha256
node --test test/byo-demo-probe.test.ts  # drives in-process + verifies the committed trace
```

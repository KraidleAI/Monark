# Derived by scripts/export-public.mjs from the governance workflow (ADR-M004 D7 bis): lot-size gate removed, push trigger added.
# MONARK — CI gates (instantiated from the compliance corpus template `templates/ci-gates.yml`,
# doc 02 v1.2; concrete tools fixed by ADR-M003 D9 and its dated addenda, R-23).
# Invariant (not parameterisable): EVERY job is BLOCKING. No `continue-on-error` (test 38 checks this
# mechanically, ADR-M003 D11).
#
# Provenance of pinned action SHAs (never guessed — GitHub API `git/ref/tags/<tag>`, cross-checked by
# the Lot V worker on 2026-09-05 and re-verified by the G2 reviewer on 2026-09-06):
#   actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1   # v7.0.1
#   actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
#
#
# Rewritten by the orchestrator on 2026-09-06 under ADR-M003 D10 (two Opus worker deaths on this lot),
# to the specification of docs/G1-lot-V.md + docs/G2-lot-V.md; English only (D0.5).

name: gates
on:
  push:
  pull_request:

jobs:
  g1-controle-generation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Pinned model identifiers only (R-1/R-4 — a bare tier resolves platform-side)
        run: bash enforcement/lint-model-pinning.sh . # vendored from the public vibegates repo (mirror 2026-08-13)

  g3-verification:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          cache: npm
      - name: Install (lockfile must be present and consistent)
        run: npm ci
      - name: Vocabulary gate + strict typecheck + tests (contracts_frozen, fixtures, tests 1-41)
        run: npm run gate:vocab && npm run typecheck && npm test

  g4-architecture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          cache: npm
      - run: npm ci
      - name: eslint recommended-type-checked (src strict) + test-debt ratchet (ADR-M003 D9 ter)
        run: npm run lint && npm run lint:ratchet

  g6-compliance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          cache: npm
      - run: npm ci
      - name: Dependency audit (high and above = failure)
        run: npm audit --audit-level=high
